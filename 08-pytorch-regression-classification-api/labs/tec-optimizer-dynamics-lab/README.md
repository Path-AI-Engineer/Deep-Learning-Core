# Optimizer dynamics lab

This bounded comparison keeps data, architecture and seed fixed while changing only optimizer
and regularization. It is evidence for one engineering decision, not hyperparameter search.

Run: `python labs/tec-optimizer-dynamics-lab/lab.py`.
