# Tensors, autograd and device lab

Demonstrates tensor `dtype`, `shape`, `device`, `requires_grad`, a forward calculation,
`backward()` and gradient reset.

Run: `python labs/tec-tensors-autograd-device-lab/lab.py`.
