# BPTT Gradient Flow Lab

Measure an initial hidden-state gradient across increasing sequence lengths under shrinking, stable, and growing recurrence. Compare norms before and after clipping; clipping limits explosions but does not repair vanishing gradients.

Run `python scripts/run_cell_labs.py`.

