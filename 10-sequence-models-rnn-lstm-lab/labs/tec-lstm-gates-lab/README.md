# LSTM Gates Lab

Inspect input, forget, candidate, and output gates at each timestep. Verify both hidden and cell state against `torch.nn.LSTMCell` with shared parameters.

Use the Sequence Lab interface or run `python scripts/run_cell_labs.py`.

