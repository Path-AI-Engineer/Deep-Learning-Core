# GRU Gates Lab

Inspect reset, update, and candidate activations. Compare the compact GRU state transition with the LSTM memory path and verify PyTorch parity.

Use the Sequence Lab interface or run `python scripts/run_cell_labs.py`.

