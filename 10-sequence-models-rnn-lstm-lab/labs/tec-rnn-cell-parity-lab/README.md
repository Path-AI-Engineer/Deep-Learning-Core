# RNN Cell Parity Lab

Reproduce a tanh RNN step in NumPy, copy the same parameters into `torch.nn.RNNCell`, and compare final hidden state. The accepted maximum absolute difference is `1e-9`.

Run `python scripts/run_cell_labs.py`.

