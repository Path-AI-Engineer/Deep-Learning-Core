# Paper assets

`scripts/build_paper_assets.py` exports tables and JSON directly from the verified
bundle. Generated assets are evidence derivatives; they are never edited manually.

The eventual report should preserve separate ID/OOD tables, seed-level results,
exclusions, cost evidence and the frozen-test access timestamp.
