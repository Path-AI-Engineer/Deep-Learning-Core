# Data augmentation lab

Inspect random crop padding only in the training pipeline. Keep validation,
test and inference deterministic. Horizontal flip is configurable but disabled
by default because its usefulness should be justified for the selected domain,
not assumed.
