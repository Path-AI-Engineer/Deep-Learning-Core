# Vision confusion-matrix lab

Read rows as observed labels and columns as predictions. Pair the matrix with
macro F1, per-class precision/recall/F1 and concrete error records. Do not infer
causes from the matrix alone; use it to locate classes and samples for review.
